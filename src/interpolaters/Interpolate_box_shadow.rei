[@genType]
let interpolateBoxShadow:
  (~range: (float, float), ~domain: (string, string), ~value: float) =>
  string;

[@genType]
let interpolateBoxShadows:
  (~range: (float, float), ~domain: (string, string), ~value: float) =>
  string;
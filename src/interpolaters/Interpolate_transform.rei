[@genType]
let interpolateTransform:
  (~range: (float, float), ~domain: (string, string), ~value: float) =>
  string;

[@genType]
let interpolateTransforms:
  (~range: (float, float), ~domain: (string, string), ~value: float) =>
  string;